Instructions on how to run:
    1. type the command "make" or "make clean" to compile, link, and run the program
    2. once compiled, you can also use the command "./lab02" to run the program
    3. clean the directory by using the command "make clean" to run the program which will remove any object files each time before recompiling.
